function  isPerfectSquare(nummber){
    if(nummber >0){
        const temp = Math.sqrt(nummber); 
        if(temp*temp==nummber){ 
            console.log(number," is  a prefer number ")
        }else{
            console.log(number," isn't a prefer number ")
        }
        // let temp1 = (temp*temp==nummber);
       
    }
}
const number=65;
isPerfectSquare(number);

